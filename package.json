{
  "name": "mysql-to-graphql",
  "version": "1.0.0",
  "description": "Create GraphQL schema from MySQL database schema",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "k4wo",
  "license": "ISC",
  "dependencies": {
    "mysql2": "^1.6.4",
    "yargs": "^12.0.2"
  }
}
